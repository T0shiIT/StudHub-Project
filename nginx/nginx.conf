# # # events {
# # #     worker_connections 1024;
# # # }
# # # http {
# # #     server {
# # #         listen 80;

# # #         location / {
# # #             proxy_pass http://frontend:5173;
# # #             proxy_set_header Host $host;
# # #             proxy_set_header X-Real-IP $remote_addr;
# # #         }
        
# # #         location /api/ {
# # #             proxy_pass http://backend:8080;
# # #             proxy_set_header Host $host;
# # #             proxy_set_header X-Real-IP $remote_addr;
# # #             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
# # #         }
# # #     }
# # # }

# # events {
# #     worker_connections 1024;
# # }
# # http {
# #     server {
# #         listen 80;

# #         location / {
# #             proxy_pass http://frontend:5173;
# #             proxy_set_header Host $host;
# #             proxy_set_header X-Real-IP $remote_addr;
# #         }
        
# #         #Временно комменчу для построения безопасности
# #         # #роут для C++
# #         # location /api/cpp/ {
# #         #     proxy_pass http://cpp:8081;
# #         #     proxy_set_header Host $host;
# #         #     proxy_set_header X-Real-IP $remote_addr;
# #         # }

# #         # #Маршрут для тестов/ролей (если он нужен тесту)
# #         # location /api/test/ {
# #         #     proxy_pass http://cpp:8081;
# #         #     proxy_set_header Host $host;
# #         # }
        
# #         # Общий API на джаву
# #         location /api/ {
# #             proxy_pass http://backend:8080;
# #             proxy_set_header Host $host;
# #             proxy_set_header X-Real-IP $remote_addr;
# #             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
# #         }
# #     }
# # }

# events {
#     worker_processes auto;
# }

# http {
#     include       /etc/nginx/mime.types;
#     default_type  application/octet-stream;

#     upstream backend {
#         server backend:8080;
#     }

#     upstream cpp {
#         server cpp:8081;
#     }

#     upstream messenger {
#         server messenger:9000;
#     }

#     server {
#         listen 80;

#         # ─── Frontend (статика) ───────────────────────────────────────────────
#         location / {
#             root /usr/share/nginx/html;
#             try_files $uri $uri/ /index.html;
#         }

#         # ─── Java backend ────────────────────────────────────────────────────
#         location /api/ {
#             proxy_pass http://backend;
#             proxy_set_header Host              $host;
#             proxy_set_header X-Real-IP         $remote_addr;
#             proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#         }

#         # ─── C++ сервис ──────────────────────────────────────────────────────
#         location /api/cpp/ {
#             proxy_pass http://cpp;
#             proxy_set_header Host            $host;
#             proxy_set_header X-Real-IP       $remote_addr;
#         }

#         # ─── Chat WebSocket (/ws/{roomID}) ───────────────────────────────────
#         location /ws/ {
#             proxy_pass http://messenger;
#             proxy_http_version 1.1;
#             proxy_set_header Upgrade    $http_upgrade;
#             proxy_set_header Connection "upgrade";
#             proxy_set_header Host       $host;
#             proxy_read_timeout 3600s;
#             proxy_send_timeout 3600s;
#         }

#         # ─── Chat HTTP (/history/, /health) ──────────────────────────────────
#         location /chat/ {
#             proxy_pass http://messenger/;
#             proxy_set_header Host            $host;
#             proxy_set_header X-Real-IP       $remote_addr;
#         }
#     }
# }


worker_processes auto;

events {}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    upstream backend { server backend:8080; }
    upstream cpp { server cpp:8081; }
    upstream messenger { server messenger:9000; }

    server {
        listen 80;

        location / {
            root /usr/share/nginx/html;
            try_files $uri $uri/ /index.html;
        }

        location /api/ {
            proxy_pass http://backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        location /api/cpp/ {
            proxy_pass http://cpp;
            proxy_set_header Host $host;
        }

        location /ws/ {
            proxy_pass http://messenger;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_read_timeout 3600s;
        }

        location /chat/ {
            proxy_pass http://messenger/;
            proxy_set_header Host $host;
        }
    }
}